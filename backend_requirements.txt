aiohttp>=3.9.5          # async HTTP client (concurrent fetching)
aiolimiter>=1.1.0       # rate limiter to avoid hammering hosts
beautifulsoup4>=4.12.3  # HTML/XML parsing
lxml>=5.2.2             # fast parser backend for BeautifulSoup
tldextract>=5.1.2       # registrable-domain detection (example.com vs sub.example.com)
yarl>=1.9.4             # safe URL object (used by aiohttp)
google-generativeai>=0.7.2 # Gemini SDK
orjson>=3.10.7          # fast JSON (parse/pretty-print)
python-slugify>=8.0.4   # (reserved; not used yet—handy for filenames/ids)
